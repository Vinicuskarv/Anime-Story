import NavBar from '../../components/navbar';
import Header from './header';

function AnimeList() {
  return (
    <div>
        <NavBar/>
        <Header/>
    </div>
  );
}

export default AnimeList;